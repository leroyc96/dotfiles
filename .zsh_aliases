function def_fun () {
  while [[ $# -ge 2 ]]; do
    eval "function $1 () { $2 \$@ }"
    shift 2
  done
}

function def_funs () {
  def_fun $funs
  unset funs
}

funs=(
  c 'clear'
  q 'exit'
  v 'vim'
  la 'ls -a'
  ll 'ls -l'
  lal 'ls -al'
  rmi 'rm -i'
  
  dev 'cd ~/dev'
  ccd 'cd ~/dev/credit_card'
  ui 'cd ~/dev/credit_card/product/dashboard'
  pg 'cd ~/dev/playground'
  
  flint 'yarn lint:fix'
  tg 'yarn types:generate'
  sb 'yarn && yarn storybook'
  dbash 'cd ~/dev/credit_card && script/exec bash'
  k 'kubectl'
  km 'brexctl init'
  protog 'brexctl gen proto'
  2fa 'pre-get-2fa'
  ys 'yarn && yarn start'
 
  g 'git'
  gb 'git branch'
  gc 'git checkout'
  gd 'git diff --cached --name-only'
  gf 'git fetch'
  gm 'git merge'
  gl 'git checkout -'
  gr 'git reset'
  gs 'git status'
  pull 'git pull'
  push 'git push'
  pushf 'git push -f'
  cmt. 'git commit -m "." && git push'
  rb 'git rebase'
  rbm 'git checkout main && git pull && git checkout - && git rebase main && git push -f'
  rbc 'git rebase --continue'
  stash 'git stash'
  stashp 'git stash pop'
  main 'git checkout main && git pull'
  prune 'git prune && git remote prune origin'

  b 'brexctl'
  remsh 'pre-remsh -p'
  remsh-stg '~/dev/credit_card/script/brex-remsh -k staging -c 3ba13b6caff343daa1329f076c006d97 -p'
  fwd-stg 'while true; do kubectl port-forward --context staging --namespace staging svc/core-pgbouncer 6666:5432; done;'
); def_funs
